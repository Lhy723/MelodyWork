{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "allow-melody-commands",
    "notification:default",
    "opener:allow-open-url",
    "process:allow-restart"
  ]
}
